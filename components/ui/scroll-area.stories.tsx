import type { Meta, StoryObj } from "@storybook/react";
import { ScrollArea } from "./scroll-area";

const meta: Meta<typeof ScrollArea> = { component: ScrollArea };
export default meta;
export const Primary: StoryObj<typeof ScrollArea> = { args: {} };
