import type { Meta, StoryObj } from "@storybook/react";
import { Tabs } from "./tabs";

const meta: Meta<typeof Tabs> = { component: Tabs };
export default meta;
export const Primary: StoryObj<typeof Tabs> = { args: {} };
