import type { Meta, StoryObj } from "@storybook/react";
import { BatchConfiguration } from "./BatchConfiguration";

const meta: Meta<typeof BatchConfiguration> = { component: BatchConfiguration };
export default meta;
export const Primary: StoryObj<typeof BatchConfiguration> = { args: {} };
