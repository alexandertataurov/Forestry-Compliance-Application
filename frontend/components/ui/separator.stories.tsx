import type { Meta, StoryObj } from "@storybook/react";
import { Separator } from "./separator";

const meta: Meta<typeof Separator> = { component: Separator };
export default meta;
export const Primary: StoryObj<typeof Separator> = { args: {} };
