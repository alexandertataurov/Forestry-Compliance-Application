import type { Meta, StoryObj } from "@storybook/react";
import { Switch } from "./switch";

const meta: Meta<typeof Switch> = { component: Switch };
export default meta;
export const Primary: StoryObj<typeof Switch> = { args: {} };
