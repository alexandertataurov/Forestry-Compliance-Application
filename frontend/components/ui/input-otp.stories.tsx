import type { Meta, StoryObj } from "@storybook/react";
import { InputOTP } from "./input-otp";

const meta: Meta<typeof InputOTP> = { component: InputOTP };
export default meta;
export const Primary: StoryObj<typeof InputOTP> = { args: {} };
