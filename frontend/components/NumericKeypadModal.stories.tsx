import type { Meta, StoryObj } from "@storybook/react";
import { NumericKeypadModal } from "./NumericKeypadModal";

const meta: Meta<typeof NumericKeypadModal> = { component: NumericKeypadModal };
export default meta;
export const Primary: StoryObj<typeof NumericKeypadModal> = { args: {} };
